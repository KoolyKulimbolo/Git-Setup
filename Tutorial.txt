git init
git remote add origin (#link)
git add .
git commit -am "komentar"
git push -u origin master
git push 
git diff 
git log
git checkout -b (#Name Brancha) (#Kod Starog Commita)
git checkout -b (#name)
git branch (#name)
git branch -d (#name)
git branch ##shows branch listing
git clone
git pull (#Kod Linka)
git rm (#FileName)
rm -rf .git 
git stash  


rebase - Sluzi za prebacivanje koda iz main brancha na feature branch, najcesce iz razloga sto je main branch uveo nove promjene koda.
feature branch postaje main kod 
merge - spaja dva brancha i prikazuje razlike/conflicte 
merge squash - spaja dva koda tako sto sve commitove iz jednog koda ugura u jedan novi commit na master ili drugom odabranom branchu
